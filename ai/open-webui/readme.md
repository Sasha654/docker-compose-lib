По умолчанию используется монтирование директории хоста (Bind Volumes):
```
./open-webui-data:/app/backend/data
```

Для использования именованного тома (Named Volumes):
```
open-webui-data:/app/backend/data
```
